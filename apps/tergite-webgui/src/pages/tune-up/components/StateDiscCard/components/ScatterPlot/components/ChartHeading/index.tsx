import { Text } from '@chakra-ui/react';
import React from 'react';

const ChartHeading = () => (
	<Text fontSize='xs' mb='1' textAlign='center'>
		State discrimination (I vs Q)
	</Text>
);

export default ChartHeading;
