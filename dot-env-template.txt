# Copy this file to a .env file in the tergite-mss folder.
# The .env file is a simple list of keys and values. It is also known as the INI file on MS Windows. 
# Fill in the necessary values.


# keep as it is
DB_NAME=milestone1

# this has to be set
BCC_MACHINE_ROOT_URL=http://host:port
DB_MACHINE_ROOT_URL=mongodb://user:pass@host:port
MSS_PORT=num
WS_PORT=num

# APP_SETTINGS reflect which environment the app is to run in.
# Options
#  - development
#  - production
#  - staging
#  - test
# Default: production
APP_SETTINGS=production

# the test BCC connection
TEST_BCC_MACHINE_ROOT_URL=http://localhost:8000
# the test mongodb connection. Ensure mongo is running
TEST_DB_MACHINE_ROOT_URL=mongodb://localhost:27017

# For datetime precisions; number of additional components of the time to include
# See https://docs.python.org/3/library/datetime.html#datetime.datetime.isoformat
# DATETIME_PRECISION=<any of 'milliseconds', 'auto', 'microseconds', 'seconds', 'minutes', 'hours'>

# Authentication options
# =====================
#
# All auth options are optional, but to use auth, you must set client ID's and secrets and the JWT secret
#
# In order to give different groups of users, different access, each group will have its
# own client
#
# Authentication with tergite admins
# See https://docs.github.com/en/apps/oauth-apps/building-oauth-apps/authorizing-oauth-apps
TERGITE_CLIENT_ID=<some-github-client-id>
TERGITE_CLIENT_SECRET=<some-github-client-secret>
# the regex validation for emails to allow for this login. default: ".*"
TERGITE_EMAIL_REGEX=".*"
# Roles are comma-separated.
# roles can be: "admin", "user", "researcher", "partner". Default is "user".
TERGITE_ROLES="admin"

# Authentication with Chalmers
# See https://learn.microsoft.com/en-us/azure/active-directory/develop/quickstart-web-app-python-sign-in?tabs=windows
CHALMERS_CLIENT_ID=<some-microsoft-client-id>
CHALMERS_CLIENT_SECRET=<some-microsoft-client-secret>
# the regex validation for emails to allow for this login. default: ".*"
CHALMERS_EMAIL_REGEX=".*"
# Roles are comma-separated.
# roles can be: "admin", "user", "researcher", "partner". Default is "user".
CHALMERS_ROLES="researcher"

# # Authentication with WACQT Partners; each partner has own set of credentials
# # e.g. assuming Erikson uses OpenID connect
# ERIKSON_CLIENT_ID=<some-openid-client-id>
# ERIKSON_CLIENT_SECRET=<some-openid-client-secret>
# ERIKSON_CONFIG_ENDPOINT=<some-openid-config-endpoint>
# # the regex validation for emails to allow for this login. default: ".*"
# ERIKSON_EMAIL_REGEX=".*"
# # Roles are comma-separated.
# # roles can be: "admin", "user", "researcher", "partner". Default is "user".
# ERIKSON_ROLES="partner"
# # to use the above configs, however, the settings file as well as the auth service have to updated

# Authentication with Puhuri
# Register an application at https://webapp.prod.puhuri.eduteams.org/sp_request
# to get the client ID and the client secret
PUHURI_CLIENT_ID=<some-puhuri-client-id>
PUHURI_CLIENT_SECRET=<some-puhuri-client-secret>
PUHURI_CONFIG_ENDPOINT=https://proxy.acc.puhuri.eduteams.org/.well-known/openid-configuration
# the regex validation for emails to allow for this login. default: ".*"
PUHURI_EMAIL_REGEX=".*"
# Roles are comma-separated.
# roles can be: "admin", "user", "researcher", "partner". Default is "user".
PUHURI_ROLES="user"

# Secret for regenerating JWT.
# It can be generated using `openssl rand -base64 45`
JWT_SECRET=<some-jwt-secret>
# JWT token lifespan in seconds. default = 3600
JWT_TTL=3600